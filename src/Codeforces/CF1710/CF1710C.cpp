#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int ____ = cin.tie(0)->sync_with_stdio(0);
#define endl '\n'

#define dbg(x) #x << " = " << (x) << ", "
#define in(x) x = (cin >> x, x)

// END OF HEADER | Author: Roger Young

int main() {
	int in(T);
	while (T--) {
		cout << "X" << endl;
	}
	return 0;
}
